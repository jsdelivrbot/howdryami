/* eslint-disable */

import React from 'react';
import StartupLoader from './startupLoader.scss';

describe('renders without crashing', () => {
  test('renders without crashing', () => {
    const wrapper = shallow(
      <StartupLoader />
    );
  })
});
